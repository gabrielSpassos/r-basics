x <- 5
y <- 3
7 -> z

x
print(z)
print(paste(x, y, z))

primos <- c(2,3,5,7,11,13,19,23,29)

# Média Aritmética
mean(primos)

sum(primos)

max(primos)

min(primos)

# Desvio Padrão
sd(primos)

# Variância
var(primos)

#mediana
median(primos)
